[
  {
    "id": "fix-price-parser",
    "prompt": "Work only in this demo directory. Run `uv run pytest -q`, diagnose the failing price parser test, make the smallest correct fix, rerun the tests, and summarize the result."
  },
  {
    "id": "validate-garbage-input",
    "prompt": "Work only in this demo directory. Improve price_parser.py so parse_price raises a ValueError with a clear message for garbage input like \"abc\" or an empty string. Add tests for the new validation, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "parse-many-prices",
    "prompt": "Work only in this demo directory. Add a parse_prices function to price_parser.py that takes a list of price strings and returns their total as a Decimal, skipping invalid entries. Add tests, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "discount-module",
    "prompt": "Work only in this demo directory. Create discount.py with an apply_discount(price, percent) function that handles 0% and 100% and raises ValueError for negative or over-100 percentages. Add test_discount.py with tests, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "currency-formatter",
    "prompt": "Work only in this demo directory. Create currency.py with a format_usd(amount) function that formats a Decimal as a string like $1,234.50 with thousands separators. Add tests including zero and negative amounts, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "invoice-receipt",
    "prompt": "Work only in this demo directory. Create invoice.py with a format_invoice(items) function that takes (name, price) tuples and returns a plain-text receipt with a total line. Add tests, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "refactor-normalize",
    "prompt": "Work only in this demo directory. Refactor price_parser.py to extract its string cleanup logic into a private helper function and reuse it. Keep the public API unchanged, make sure `uv run pytest -q` stays green, and summarize."
  },
  {
    "id": "negative-amounts",
    "prompt": "Work only in this demo directory. Extend parse_price in price_parser.py to also accept negative amounts written as -$5.00 and accounting-style ($5.00). Add regression tests, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "sales-tax",
    "prompt": "Work only in this demo directory. Create tax.py with an add_tax(price, rate) function that adds sales tax and rounds to cents using ROUND_HALF_UP. Add tests for typical rounding edge cases, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "document-workspace",
    "prompt": "Work only in this demo directory. Write a README.md for this directory that documents every Python module present, shows one usage example for each, and notes how to run the tests. Run `uv run pytest -q` to confirm everything still passes, and summarize."
  },
  {
    "id": "split-bill",
    "prompt": "Work only in this demo directory. Create split_bill.py with a split_bill(total, people) function that splits a Decimal total across people so the shares differ by at most one cent and always sum back to the total. Add tests, run `uv run pytest -q`, and summarize."
  },
  {
    "id": "cli-entry-point",
    "prompt": "Work only in this demo directory. Add a main() entry point to price_parser.py so running `uv run python price_parser.py '$12.50' '€7.99'` prints each parsed amount on its own line. Run it once to verify, run `uv run pytest -q`, and summarize."
  }
]
